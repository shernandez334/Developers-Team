package org.example.persistance;

public class Room {

    /*public Room(int element_id, int quantity, double price, String theme, String diffLevel){
        super(name, element_id, quantity, price, theme);
        this.diffLevel = diffLevel;
    }

    public static Room createRoom(String name, int element_id, int quantity, double price, String theme, String diffLevel){
        return new Room(name, element_id, quantity, price, theme, diffLevel);
    }

    @Override
    public String dataInfo() {
        return "INSERT INTO room (element_id, type, quantity, price, theme, difficulty) " +
                "VALUES ('" + super.getElement_id() + ", 'ROOM', " +
                super.getQuantity() + ", " + super.getPrice() + ", '" + super.getTheme() +
                "', '" + this.diffLevel + "');";
    }*/
}
